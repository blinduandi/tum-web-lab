{
  "name": "pagebound-api",
  "version": "1.0.0",
  "description": "REST API back-end for Pagebound (Lab 6) — JWT auth, CRUD, pagination, Swagger UI",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.1",
    "jsonwebtoken": "^9.0.2",
    "swagger-ui-express": "^5.0.1",
    "yaml": "^2.6.1"
  },
  "license": "MIT"
}
